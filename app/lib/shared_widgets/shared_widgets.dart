/// Component library barrel (FA §13). Features import this, never individual
/// widget files — keeps the library surface reviewable in one place.
library;

export 'app_button.dart';
export 'app_card.dart';
